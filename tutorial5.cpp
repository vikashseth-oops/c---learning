#include<iostream>
#include<iomanip>

 using namespace std;

int c = 41;
 int main()
  {
  //  int a,b,c;
  //  cout<<"the value of a is"<<endl;
  //  cin>>a;
  //  cout<<"the value of b is"<<endl;
  //  cin>>b;
  //  c=a+b;
  //  cout<<"the value of c is"<<c<<endl;
  //  cout<<"the value of c is"<<::c<<endl;
  // int p =1,q =12,r =1234;
  // cout<<"the value of p without setw is; "<<p<<endl;
  // cout<<"the value of q without setw is; "<<p<<endl;
  // cout<<"the value of r without setw is; "<<p<<endl;
  // cout<<"the value of p with setw is; "<<setw(4)<<p<<endl;
  // cout<<"the value of q with setw is; "<<setw(4)<<q<<endl;
  // cout<<"the value of r with setw is; "<<setw(4)<<r<<endl;
  
  int p=1,q=12,r=1234;
 cout<<"the value of p without setw is; "<<p<<endl;
  cout<<"the value of q without setw is; "<<q;
  cout<<"\nthe value of r without setw is; "<<r<<endl;
  cout<<"the value of p with setw is; "<<setw(4)<<p<<endl;
  cout<<"the value of q with setw is; "<<setw(4)<<q<<endl;
  cout<<"the value of r with setw is; "<<setw(4)<<r<<endl;
  
  const int a = 25;
  cout<<"the value of a was:"<<a<<endl;
  cout<<"now the value of a is:"<<a;
    return 0;
 }
 